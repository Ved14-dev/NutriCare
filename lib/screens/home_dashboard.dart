// Forwarding stub to the cleaned dashboard implementation.
// Keeping this small stub avoids breaking existing imports that refer to
// `lib/screens/home_dashboard.dart` while the cleaned implementation lives
// in `home_dashboard_clean.dart`.

export 'home_dashboard_clean.dart';
